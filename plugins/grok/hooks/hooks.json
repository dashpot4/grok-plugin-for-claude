{
  "description": "Tags grok-companion jobs with the Claude Code session id so /grok:status and /grok:result are scoped to the session that created them.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/session-lifecycle-hook.mjs\" SessionStart",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
